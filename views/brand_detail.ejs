<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <%- include('partials/sidebar'); %>
    <div class="content">
      <h1><%= brand.name %></h1>
      <% if(brand_items.length > 0) { %>
      <ul>
        <% for(let brand of brand_items) {%>
        <li>
          <a href="<%=brand.url%>"><strong> <%= brand.name %> </strong></a>
          <p><%= brand.description %></p>
        </li>
        <% } %>
      </ul>
      <% } else { %>
      <p>There are no items associated to this brand</p>
      <% } %>
      <hr />
      <p class="delete"><a href="<%= brand.url +'/delete' %>">Delete</a></p>
      <p class="update"><a href="<%= brand.url +'/update' %>">Update</a></p>
    </div>
  </body>
</html>
