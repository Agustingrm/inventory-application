<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body>
    <%- include('partials/sidebar'); %>
    <div class="content">
      <% if(category_items.length) { %>
      <p><strong>Delete the following items before attempting to delete this category</strong></p>
      <h4>Items</h4>
      <% for (let item in category_items) { %>
      <dl>
        <dt><a href="<%= category.url %>"><%= category_items[item].name %></a></dt>
        <dl><%= category_items[item].description%></dl>
      </dl>
      <% } %> <% } else { %>
      <p>Do you really want to delete this category?</p>
      <form method="POST">
        <input type="hidden" id="categoryid" name="categoryid" required="true" value="<%= category._id%>" />
        <input type="submit" value="Delete" />
      </form>
      <% } %>
    </div>
  </body>
</html>
