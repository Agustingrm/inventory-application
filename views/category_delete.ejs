<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <%- include('partials/sidebar'); %>
    <div class="content">
      <% if(category_items) { %>
      <p><strong>Delete the following items before attempting to delete this category</strong></p>
      <h4>Items</h4>
      <%= category_items %>
      <% for (let item in category_items) { %>
      <dl>
        <dt><a href="<% category.url %>"><% category.name %></a></dt>
        <dl><% category.description %></dl>
      </dl>
      <% } %> <% } else { %>
      <p>Do you really want to delete this category?</p>
      <form method="POST">
        <input type="hidden" id="categoryid" name="categoryid" required="true" value="<%= category._id%>" />
        <input type="submit" value="Delete" />
      </form>
      <% } %>
    </div>
  </body>
</html>
