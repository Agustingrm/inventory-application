<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <%- include('partials/sidebar'); %>
    <div class="content">
      <h1><%= title %></h1>
      <p>Welcome to <%= category.name %></p>
      <ul>
        <% for(let category of category_items) {%>
        <li>
          <a href="<%=category.url%>"><strong> <%= category.name %> </strong></a>
          <p><%= category.description %></p>
        </li>
        <% } %>
      </ul>
    </div>
  </body>
</html>
